import time.time from time
currencycount = 0
starttime=time.time()
def Fortclick():
    currencycount += click_power
def Clickrate():
    While True:
        time.sleep(0.1-((time.time() - starttime) % 0.1))
        # next need to generate total of all producers and them to currency count. Use generator expressions and lists
def Buyresearch(x):
    currencycount -= x.cost
    exec(x.effect)
    #this is kinda dodgy, apparently execute is a shitty command to use
def BuyProducer(x):
    currencycount -= x.cost
    x.number += 1
class Research:
    def __init__(name, effect, cost):
        self.name = name
        self.effect = effect
        self.cost = cost
class Producer:
    def __init__(name, image, productionrate, number, cost):
        self.name = name
        self.image = image
        self.productionrate = productionrate
        self.number = number
        self.cost = cost
        self.netproduction = productionrate*number

exampleresearch = Research(Example, szeproducer.productionrate = 2*szeproducer.productionrate, 100)
exampleproducer = Producer(szeproducer, sze.png, 0.1, 0, 10)
#### This whole area is WIP
